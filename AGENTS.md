# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently a blank-slate project with a single `start.md` file. There are no applications, services, dependencies, build tools, tests, or CI/CD pipelines.

- **No package manager or dependencies** — no `package.json`, `requirements.txt`, `Gemfile`, `go.mod`, or similar files exist.
- **No build or lint steps** — no `Makefile`, bundler config, or linter config is present.
- **No Docker or devcontainer setup** — no `Dockerfile`, `docker-compose.yml`, or `.devcontainer/` directory.
- **No services to start** — there are no backend, frontend, or database services.

When application code is added, update this file with setup, build, lint, test, and run instructions.
